spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/news-management
    username: root
    password: root
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
test:
  server: 'http://localhost:'